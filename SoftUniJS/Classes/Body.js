// we define class members like properties and methods.
// using opening and closing curly braces: {...}

class SupportEmployee {
    constructor(name) {
        this.name = name;
        this.department = 'support';
    }
}